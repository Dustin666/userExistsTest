<h1>Listing exists</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @exists.each do |exist| %>
  <tr>
    <td><%= link_to 'Show', exist %></td>
    <td><%= link_to 'Edit', edit_exist_path(exist) %></td>
    <td><%= link_to 'Destroy', exist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Exist', new_exist_path %>
